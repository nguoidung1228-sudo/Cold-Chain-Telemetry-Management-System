import { useState } from 'react'
import { PageHeader } from '../components/PageHeader'
export function SettingsPage() { const [saved, setSaved] = useState(false); return <><PageHeader eyebrow="Workspace configuration" title="Settings" description="Manage monitoring policies and notification preferences." /><div className="detail-grid"><section className="card detail-card"><h2>Organization profile</h2><p style={{ color: 'var(--muted)', fontSize: 13, marginTop: 5 }}>These details appear in exported reports.</p><div className="form-grid" style={{ marginTop: 22 }}><label className="field">Organization name<input defaultValue="Cold Chain Operations" /></label><label className="field">Timezone<select defaultValue="Asia/Singapore"><option>Asia/Singapore</option><option>Asia/Jakarta</option><option>Asia/Tokyo</option></select></label><label className="field">Primary contact<input defaultValue="operations@coldchain.local" /></label><label className="field">Data retention<select defaultValue="90 days"><option>30 days</option><option>90 days</option><option>1 year</option></select></label></div></section><aside><section className="card detail-card"><h2>Temperature policy</h2><p style={{ color: 'var(--muted)', fontSize: 13 }}>Alerts trigger when readings leave this range.</p><div className="form-grid" style={{ marginTop: 20 }}><label className="field">Minimum °C<input defaultValue="2" type="number" /></label><label className="field">Maximum °C<input defaultValue="8" type="number" /></label></div><div style={{ marginTop: 22, display: 'grid', gap: 13, fontSize: 13 }}><label><input type="checkbox" defaultChecked /> Email critical alerts</label><label><input type="checkbox" defaultChecked /> Daily operations digest</label><label><input type="checkbox" /> SMS escalation</label></div></section><button className="btn btn-primary" onClick={() => setSaved(true)}>Save settings</button>{saved && <div className="toast">Settings saved successfully</div>}</aside></div></> }
