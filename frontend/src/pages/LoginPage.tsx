import { Snowflake, ShieldCheck } from 'lucide-react'
import { useNavigate } from 'react-router-dom'
import { useState } from 'react'
import { useAuthStore } from '../stores/authStore'
export function LoginPage() {
  const [email, setEmail] = useState('alex@frostline.io'); const [password, setPassword] = useState('password'); const login = useAuthStore((state) => state.login); const navigate = useNavigate()
  return <div style={{ minHeight: '100vh', display: 'grid', placeItems: 'center', padding: 20, background: 'linear-gradient(135deg,#e8f8f4,#edf4fb)' }}><div className="card" style={{ width: '100%', maxWidth: 420, padding: 34 }}><div className="brand" style={{ margin: '0 0 28px', color: '#12263f' }}><span className="brand-mark"><Snowflake size={20} /></span>Cold Chain<span style={{ color: '#16a6a1' }}>.</span></div><div className="eyebrow">Cold-chain intelligence</div><h1 style={{ fontSize: 27, marginTop: 8 }}>Welcome back</h1><p style={{ color: 'var(--muted)', fontSize: 14, marginBottom: 25 }}>Sign in to monitor every shipment, sensor, and alert.</p><form onSubmit={(event) => { event.preventDefault(); login(email, password); navigate('/dashboard') }} style={{ display: 'grid', gap: 15 }}><label className="field">Work email<input value={email} onChange={(event) => setEmail(event.target.value)} type="email" required /></label><label className="field">Password<input value={password} onChange={(event) => setPassword(event.target.value)} type="password" required /></label><button className="btn btn-primary" style={{ justifyContent: 'center', padding: 12 }} type="submit">Sign in to dashboard</button></form><div style={{ display: 'flex', gap: 8, color: 'var(--muted)', fontSize: 12, alignItems: 'center', marginTop: 24 }}><ShieldCheck size={16} color="#16a6a1" /> Demo mode enabled · backend optional</div></div></div>
}
